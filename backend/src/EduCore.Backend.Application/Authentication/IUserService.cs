using EduCore.BackEnd.Application.DTOs.Response;
using EduCore.BackEnd.SharedKernel;
using System.Security.Claims;

namespace EduCore.BackEnd.Application.Authentication
{
    public interface IUserService
    {
        //Task<Result<AccountResponse>> FindByIdAsync(string userId);
        //Task<Result<AccountResponse>> FindByEmailAsync(string email);
        //Task<string> GetUserIdAsync(ClaimsPrincipal principal);
        //Task<string> GetEmailAsync(ClaimsPrincipal principal);
        //Task<string> GetUserNameAsync(ClaimsPrincipal principal);
        //Task<string> GetPhoneNumberAsync(ClaimsPrincipal principal);
        //Task<Result> ChangeEmailAsync(ClaimsPrincipal principal, string email, string code);
        //Task<Result> ChangePasswordAsync(ClaimsPrincipal principal, string oldPassword, string newPassword);
        //Task<Result> IsEmailConfirmedAsync(string email);
        //Task<Result> HasPasswordAsync(ClaimsPrincipal principal);
        //Task<Result> SetPhoneNumberAsync(ClaimsPrincipal principal, string phoneNumber);
        //Task<Result> AddPasswordAsync(ClaimsPrincipal principal, string newPassword);
    }
}