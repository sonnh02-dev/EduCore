using EduCore.BackEnd.Application.DTOs.Request;
using EduCore.BackEnd.SharedKernel;
using System.Security.Claims;
using System.Threading.Tasks;

namespace EduCore.BackEnd.Application.Authentication
{
    public interface IAuthService
    {
        //Task<Result> SignInAsync(SignInRequest request);
        //Task SignOutAsync();
        //Task<ApplicationUserDto?> GetCurrentUserAsync(ClaimsPrincipal principal);
        //Task<AuthenticationResponse> SignUpAsync(SignUpRequest request);
        //Task<AuthenticationResponse> ChangePasswordAsync(ClaimsPrincipal principal, string currentPassword, string newPassword);
        //Task<AuthenticationResponse> ResetPasswordAsync(ResetPasswordRequest request);
        //Task<TokenResponse> GeneratePasswordResetTokenAsync(string email);
        //Task<TokenResponse> GenerateEmailConfirmationAsync(ClaimsPrincipal principal);
        //Task<AuthenticationResponse> ConfirmEmailAsync(EmailConfirmationRequest request);
        //Task RefreshSignInAsync(ClaimsPrincipal principal);
        //Task<TokenResponse> GenerateEmailChangeAsync(ClaimsPrincipal principal, string newEmail);
    }
}
